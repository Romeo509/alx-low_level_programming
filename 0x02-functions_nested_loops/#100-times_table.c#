#include "main.h"

/**
* print_times_table - Print multiplication table for factor of n
* @n: Integer n
*/
void print_times_table(int n)
{
if (n < 0 || n > 15)
return;
for (int i = 0; i <= n; i++)
{
for (int j = 0; j <= n; j++)
{
int product = i *
xx
}
}

/**
* main - check the code.
*
* Return: Always 0.
*/
int main(void)
{
print_times_table(3);
